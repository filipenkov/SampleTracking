The atlassian-rest-test-jar.jar is actually a build of the
module com.atlassian.plugins.rest:atlassian-rest-test-jar copied in
the resources directory.

You might need to update it if you change the tests or the jar.