This directory contains QUnit Integration tests that run against an Edit Issue page.

Tests should not cause the browser to leave the Edit Issue page - multiple test files should be
able to run against a single browser screen... ideally.