This directory contains QUnit Integration tests that run against a View Issue page.

Tests should not cause the browser to leave the View Issue page - multiple test files should be
able to run against a single browser screen... ideally.