##disable_html_escaping() TODO REENABLE
#if ($value)
#if ($urlPrefix && $urlPrefix != '')
    <a href="${urlPrefix}">$textutils.htmlEncode($value.label)</a>
#else
    $textutils.htmlEncode($value.label)
#end
#end