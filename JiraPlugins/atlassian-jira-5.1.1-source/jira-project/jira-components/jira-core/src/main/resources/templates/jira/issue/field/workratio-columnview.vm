##disable_html_escaping() TODO REENABLE
#if ($workratio)
    #if (${workratio.intValue()} > 100)
        <span class="warning">${workratio}%</span>
    #else
        ${workratio}%
    #end
#else
&nbsp;
#end